Node.h contains Node class and methods' prototypes
Praser.h contains Parser class and methods' prototypes
Token.h contains enum for TokenType and struct TokenRecord and external tokens vector (tokens to be parsed)
Scanner.cpp contains scanner function, printSyntaxTree that prints syntax tree from root node and readTokenFiles that takes a .txt file formatted as token value, token type and creates tokens out of it and stores them in the extern vector
and the main function
